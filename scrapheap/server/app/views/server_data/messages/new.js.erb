$(".modal").modal("hide");
$("body").append(
  "<%= j modal( icon_text( 'New message', 'fa-plus' ), render( 'server_data/messages/form' ) ) %>"
);
$(".modal").on("hidden.bs.modal", function(){$(this).remove();});
$(".modal").modal("show");
