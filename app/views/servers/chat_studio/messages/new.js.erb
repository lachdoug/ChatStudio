$(".modal").modal("hide");
$("body").append(
  "<%= j modal( icon_text( 'New message', 'fa-plus' ), render( '/servers/chat_studio/messages/form' ) ) %>"
);
$(".modal").on("hidden.bs.modal", function(){$(this).remove();});
$(".modal").modal("show");
