<%= simple_form_for @message, url: servers_chat_studio_messages_path, remote: true do |f| %>
  <%= f.association :room, label_method: :name, value_method: :id, prompt: 'Select a room' %>
  <%= f.input :body %>
  <%= f.button :submit %>
<% end %>
