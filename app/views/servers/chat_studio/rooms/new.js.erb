$(".modal").modal("hide");
$("body").append(
  "<%= j modal( icon_text( 'New room', 'fa-plus' ), render( '/servers/chat_studio/rooms/form' ) ) %>"
);
$(".modal").on("hidden.bs.modal", function(){$(this).remove();});
$(".modal").modal("show");
