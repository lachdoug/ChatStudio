$(".modal").modal("hide");
$("body").append(
  "<%= j modal( icon_text( 'New client app', 'fa-plus' ), render( 'client_apps/form' ) ) %>"
);
$(".modal").on("hidden.bs.modal", function(){$(this).remove();});
$(".modal").modal("show");
